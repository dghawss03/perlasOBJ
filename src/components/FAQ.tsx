import { useState } from 'react'; import { Plus } from 'lucide-react'; import { faqs } from '../data/content'
export function FAQ(){const [active,setActive]=useState(0); return <div className="faq-list">{faqs.map(([q,a],i)=>{const answerId=`faq-answer-${i}`;return <div className={`faq ${active===i?'active':''}`} key={q}><button type="button" onClick={()=>setActive(active===i?-1:i)} aria-expanded={active===i} aria-controls={answerId}><span>{String(i+1).padStart(2,'0')}</span>{q}<Plus aria-hidden="true"/></button><div className="answer" id={answerId}><p>{a}</p></div></div>})}</div>}
