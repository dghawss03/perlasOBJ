import { useEffect, useState } from 'react'; import { Menu, X, Phone, ArrowUpRight, ChevronDown } from 'lucide-react'; import { Link, NavLink } from 'react-router-dom'
export function Header(){const [open,setOpen]=useState(false);const [scrolled,setScrolled]=useState(false);useEffect(()=>{const fn=()=>setScrolled(scrollY>24);addEventListener('scroll',fn);return()=>removeEventListener('scroll',fn)},[]);const close=()=>setOpen(false);return <header className={`header ${scrolled?'is-scrolled':''}`}><a className="skip" href="#main">Zum Inhalt</a><div className="nav-wrap"><Link to="/" className="brand" aria-label="Perlas Startseite" onClick={close}><span className="brand-mark">P</span><span>PERLAS<small>FACILITY SERVICES</small></span></Link><nav className={open?'open':''} aria-label="Hauptnavigation"><NavLink to="/" onClick={close}>Startseite</NavLink><div className="nav-dropdown"><NavLink to="/leistungen" onClick={close}>Leistungen <ChevronDown/></NavLink><div>{[['Objektbetreuung','objektbetreuung'],['Gebäudereinigung','gebaeudereinigung'],['Außenanlagenpflege','aussenanlagenpflege'],['Winterdienst','winterdienst'],['Facility Services','facility-services']].map(([n,s])=><Link key={s} to={`/leistungen/${s}`} onClick={close}>{n}</Link>)}<Link to="/leistungen" onClick={close}><b>Alle Leistungen</b></Link></div></div><NavLink to="/branchen" onClick={close}>Branchen</NavLink><NavLink to="/ueber-uns" onClick={close}>Über uns</NavLink><NavLink to="/karriere" onClick={close}>Karriere</NavLink><NavLink to="/blog" onClick={close}>Blog</NavLink><NavLink to="/kontakt" onClick={close}>Kontakt</NavLink><a className="mobile-phone" href="tel:+491776867145"><Phone/> 0177 6867145</a></nav><div className="nav-actions"><a className="phone" href="tel:+491776867145"><Phone/> 0177 6867145</a><Link className="button button-small" to="/kontakt">Anfrage stellen <ArrowUpRight/></Link><button className="menu" onClick={()=>setOpen(!open)} aria-label="Menü öffnen" aria-expanded={open}>{open?<X/>:<Menu/>}</button></div></div></header>}
