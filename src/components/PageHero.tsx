import { assetUrl } from '../utils/assets'
export function PageHero({kicker,title,text,image='/images/perlas-hero-concept.png',imageAlt='Mitarbeiter bei der Betreuung einer modernen Gewerbeimmobilie'}:{kicker:string;title:string;text:string;image?:string;imageAlt?:string}){return <section className="page-hero"><div><span className="eyebrow light">{kicker}</span><h1>{title}</h1><p>{text}</p></div><img src={assetUrl(image)} alt={imageAlt}/></section>}
