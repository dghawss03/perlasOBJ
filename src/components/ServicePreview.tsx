import { ArrowRight } from 'lucide-react'; import { Link } from 'react-router-dom'; import type { Service } from '../data/services'; import { assetUrl } from '../utils/assets'
export function ServicePreview({items,compact=false}:{items:Service[];compact?:boolean}){return <div className={`editorial-grid ${compact?'compact':''}`}>{items.map(s=><article className="editorial-card" key={s.slug}><Link to={`/leistungen/${s.slug}`} className="editorial-image"><img src={assetUrl(s.image)} alt={s.imageAlt} loading="lazy"/></Link><span>{s.category}</span><h3><Link to={`/leistungen/${s.slug}`}>{s.title}</Link></h3><p>{s.summary}</p><Link className="simple-link" to={`/leistungen/${s.slug}`}>{s.title} kennenlernen <ArrowRight/></Link></article>)}</div>}
