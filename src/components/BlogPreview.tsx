import { ArrowRight } from 'lucide-react'; import { Link } from 'react-router-dom'; import { postImageAlt, type Post } from '../data/blog'; import { assetUrl } from '../utils/assets'
export function BlogPreview({items}:{items:Post[]}){return <div className="editorial-grid blog-editorial">{items.map(p=><article className="editorial-card" key={p.slug}><Link to={`/blog/${p.slug}`} className="editorial-image"><img src={assetUrl(p.image)} alt={postImageAlt(p)} loading="lazy"/></Link><span>{p.category} · {p.date}</span><h3><Link to={`/blog/${p.slug}`}>{p.title}</Link></h3><p>{p.teaser}</p><Link className="simple-link" to={`/blog/${p.slug}`}>{p.title} lesen <ArrowRight/></Link></article>)}</div>}
